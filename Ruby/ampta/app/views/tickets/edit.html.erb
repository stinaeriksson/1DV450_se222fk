<h1> Redigera ticket</h1>

<% if @ticket.errors.any? %>
	<div id="error_messages">
		<h2><%= pluralize(@ticket.errors.count, "fel", "fel") %> i formuläret gjorde att ticketen inte kunde sparas:</h2>
	<ul>
		<% @ticket.errors.each do |attr,msg| %>
			<li><%= msg %></li>
		<% end %>
	</ul>
</div>
<% end %>

<%= form_for @ticket do |f| %>
	<div class="text_field">
		<%= f.label :Namn %>
		<%= f.text_field :name %>
	</div>

	<div class="text_area">
		<%= f.label :Beskrivning %>
		<%= f.text_area :description %>
	</div>

	<div class="text_field">
		<%= f.label :start_time %>
		<%= f.text_field :start_time %>
	</div>

	<div class="text_field">
		<%= f.label :end_time %>
		<%= f.text_field :end_time  %>
	</div>


	<div class="select">
		<%= f.select :status_id, options_from_collection_for_select(Status.all, :id, :status_name) %>
	</div>


	<div class="submit">
		<%= f.submit "Spara ändringar" %>
	</div>

<% end %>